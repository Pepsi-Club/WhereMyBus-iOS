import ProjectDescription
import ProjectDescriptionHelpers

let project = Project.makeProject(
    name: "{{ name }}Feature",
    moduleType: .feature,
    dependencies: [
        .featureDependency
    ]
)

let project = Project(
    name: "{{ name }}",
    options: .options(automaticSchemesOptions: .disabled)
) {
    Feature(name: "{{ name }}Feature") {
        FeatureDependency()
    }
    SampleApp(name: "{{ name }}Feature") {
        Feature(name: "{{ name }}Feature")
    }
    SampleAppScheme(name: "{{ name }}Feature")
}
